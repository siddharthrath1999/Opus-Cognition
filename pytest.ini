[pytest]
minversion = 7.0
addopts = -ra -q
testpaths =
    tests
# EXPLICITLY PREVENT AUTOMATED DISCOVERY OF DANGEROUS LOGIC BOMBS
norecursedirs = tests/fixtures skills packages playground
python_files = run_suite.py
